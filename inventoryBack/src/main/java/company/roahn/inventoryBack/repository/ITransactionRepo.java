package company.roahn.inventoryBack.repository;

import company.roahn.inventoryBack.models.Transaction;

import java.util.UUID;

public interface ITransactionRepo extends IGenericRepo<Transaction, UUID> {
}
