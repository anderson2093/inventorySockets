package company.roahn.inventoryBack.repository;

import company.roahn.inventoryBack.models.Inventory;

import java.util.UUID;

public interface IInventoryRepo extends IGenericRepo<Inventory, UUID> {
}
